<h1>Listing wks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wks.each do |wk| %>
  <tr>
    <td><%= link_to 'Show', wk %></td>
    <td><%= link_to 'Edit', edit_wk_path(wk) %></td>
    <td><%= link_to 'Destroy', wk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wk', new_wk_path %>
